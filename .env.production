# Production Settings for Render
DEBUG=False
SECRET_KEY=your-generated-secret-key-from-render

# Render will provide DATABASE_URL automatically
# No need to set DB_NAME, DB_USER, etc.

# Email Settings (set these in Render dashboard)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password  # Use App Password, not regular password
DEFAULT_FROM_EMAIL=Ambertek Exports <your-email@gmail.com>